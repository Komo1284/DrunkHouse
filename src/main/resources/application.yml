spring:
  application:
    name: DrunkHouse

  #데이터베이스 설정
  datasource:
    url: jdbc:h2:tcp://localhost/~/drunkhouse
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

##s3
#cloud:
#  aws:
#    region:
#      static: ap-northeast-2
#    stack:
#      auto: false
#    s3:
#      bucket: itbank-pethub-s3

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace


